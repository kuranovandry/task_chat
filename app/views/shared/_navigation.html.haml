%nav.navbar.navbar-default{role: "navigation"}
  %ul.nav.navbar-nav.navbar-right
    %li= link_to  "Workers page", root_path
    - if user_signed_in?
      %li= link_to  "#{current_user.full_name} Logout", destroy_user_session_path, method: :delete, class: 'navbar-link'
    - else
      %li= link_to "Sign up", new_user_registration_path, class: 'navbar-link'
      %li= link_to "Login", new_user_session_path, class: 'navbar-link'
